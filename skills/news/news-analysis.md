---
name: news-analysis
description: 新闻雷达事业部的热点分析方法论。当 News-003-分析 Agent 对采集到的新闻进行深度分析时激活。
assigned_agents:
  - News-003-分析
---

# 热点分析方法论

## 分析框架

### 1. 事件分类

将采集到的新闻按以下维度归类：

- **宏观经济**: 利率、CPI、就业、GDP、贸易政策
- **监管政策**: SEC/CFTC 执法、各国加密立法、央行 CBDC 动态
- **项目动态**: 技术升级、合并/分叉、合作关系、代币经济调整
- **市场结构**: 交易所动态、ETF 资金流、期权到期、清算事件
- **安全事件**: 黑客攻击、漏洞披露、跑路/冻结
- **舆论/社区**: KOL 言论、社区治理提案、Meme 趋势

### 2. 影响力评估矩阵

对每条 🔴高影响 新闻进行矩阵评估：

- **时间维度**: 短期（24h 内）/ 中期（1-4 周）/ 长期（1 个月+）
- **范围维度**: 单一资产 / 板块级 / 全市场
- **确定性**: 已确认 / 高概率 / 待确认

### 3. 关联分析

对同一时段的多条新闻进行交叉分析：

- 是否存在事件链条？（如：美联储表态→美元指数→BTC 价格）
- 是否存在矛盾信号？（如：利好政策 vs 大额抛售）
- 历史类比：过去是否发生过类似事件组合？当时市场反应如何？

### 4. 输出格式

```
## 热点分析

### 今日核心叙事
[1-2 句话描述今天最重要的市场主线]

### 重点事件深度分析
#### [事件 1 标题]
- **事件概述**: ...
- **影响评估**: 时间[短/中/长] × 范围[单一/板块/全市场] × 确定性[已确认/高概率/待确认]
- **历史类比**: ...
- **市场含义**: ...
- **关注指标**: [列出后续应跟踪的具体指标]

### 交叉关联
[不同事件之间的逻辑关系分析]

### 风险提示
[今日需要特别警惕的风险因素]
```

## 输出格式禁令(Phase 5b 批 2 / Hotfix-v9)

报告 markdown 必须严格遵守。Code 在 PDF 渲染前会预处理剥离这些字段(server/src/services/openai-post-run.ts:stripReportMetadata),但仍**禁止 Agent 输出**——双层防御。

### 禁止输出(头部噪音)
- ❌ `[董事长指令] ...` 前缀(不复述用户原指令)
- ❌ `**报告编号**: XXX`
- ❌ `**报告生成时间**: XXX`(时间已在 📅 顶部 metadata)
- ❌ `**数据窗口**: XXX`
- ❌ `**报告负责人**: XXX`
- ❌ `**数据来源**: XXX(状态码 / 响应时间)`(数据 inline 引用来源即可)
- ❌ 与 PDF 大标题重复的独立 H1(大标题由 Code 用 `📌 主题 | YYYY-MM-DD` 自动生成)

### 禁止输出(尾部)
- ❌ `--- 报告结束 ---` / `## 报告结束` / `**报告结束**`
- ❌ "四、数据质量说明" 整节(数据可靠性逐项 inline 标注)
- ❌ "免责声明" / "本报告由 AI 生成" 类样板

### 必填顶部 2 行
- ✅ `📌 主题: <简短主题,≤25 字,不含日期>`
- ✅ `📅 时间: YYYY-MM-DD`

### 表格规范
- 标准 markdown table:`| 列 | 列 |` + 分隔行 `|---|---|`
- Code 自动解析为 PDF 网格(renderTable)
- 不用 ASCII 艺术对齐(变宽字体下错位)

### 强调规范
- 加粗用 `**xxx**`,Code 自动剥离 markers(CJK 字体无 bold variant,visually 与正文同)
- 斜体用 `*xxx*`,同样剥离 markers
- 行内代码用 backtick `` `xxx` ``,同样剥离 markers
- 不用 `_xxx_` 下划线变体(Agent LLM 输出不稳定)

### 列表 / 序号
- 无序:`- xxx` 或 `* xxx`,Code 渲染为 `•` 项目符号
- 有序:`1. xxx` `2. xxx`,Code 保留数字 prefix
- 不用 `☐` / `▢` / `□` 等不常见 box-drawing 符号(CJK 字体可能缺字)

### 项目简介(每个具体项目 1-2 句)
- GitHub repo / 加密币种 / 新闻条目 / 舆情事件 / 公司:**功能 + 解决的问题**
- 条目 < 5:全部详细
- 条目 ≥ 10:Top 5 详细,其余 1 行简述
